make:
	javac -d bin/ client/*.java
	javac -d bin/ server/*.java
	# Do not change

run_client:
	java -cp bin/ client/Hello		# Change to name of the "conducting" class

run_server:
	java -cp bin/ server/Game		# Change to name of the "conducting" class

clean:
	rm -rf bin
